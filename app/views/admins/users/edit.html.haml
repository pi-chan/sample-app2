%h1 ユーザー情報の変更

= form_for(@user, url: admins_user_path(@user), html:{method: :put, role: "form", class: "col-xs-6"}) do |f|
  = render partial: "error_messages", locals:{object:@user}
  .form-group
    = f.label :email, "Eメール"
    = f.email_field :email, class:"form-control"
  - if Devise.mappings[:user].confirmable? && @user.pending_reconfirmation?
    .form-control
      メールアドレス #{@user.unconfirmed_email} が確認できていません。
  .form-group
    = f.label :name, "ニックネーム"
    = f.text_field :name, :autofocus => true, class:"form-control"
  .form-group
    = f.label :ship_name, "配送先お名前"
    = f.text_field :ship_name, class:"form-control"
  .form-group
    = f.label :ship_zip_code, "郵便番号"
    = f.text_field :ship_zip_code, class:"form-control"
  .form-group
    = f.label :ship_address, "ご住所"
    = f.text_field :ship_address, class:"form-control"
  .form-group
    = link_to "戻る", :back, role: "button", class: "btn btn-default"
    = f.submit "更新", class:"btn btn-primary"
 
