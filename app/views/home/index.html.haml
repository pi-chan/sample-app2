%h1 Home#index
%p Find me in app/views/home/index.html.haml

.header
  %ul.nav.nav-pills.pull-right
    %li= link_to "トップページ", root_path
    - if user_signed_in?
      %li.dropdown
        %a(href="#" class="dropdown-toggle" data-toggle="dropdown") 設定 <b class="caret"></b>
        %ul.dropdown-menu
          %li= link_to "アカウント管理", edit_user_registration_path(@user)
          %li= link_to "ログアウト", destroy_user_session_path, method: :delete
    - else
      %li= link_to "ユーザー登録", new_registration_path(:user)
      %li= link_to "ログイン", new_user_session_path
  %h3.text-muted= link_to "さくらマーケット", root_path
