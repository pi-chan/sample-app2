%h1 ショッピングカート

- if @cart.cart_products.empty?
  %p ショッピングカートは空です。
- else
  - @editable = true
  %table.table.table-striped
    = render @cart.cart_products
  .col-xs-6
    = render partial: "carts/cart_info", locals: {cart: @cart}
  .col-xs-6
    = link_to "購入手続きへ進む", new_user_purchase_path(current_user), role: "button", class: "btn btn-primary btn-large"
